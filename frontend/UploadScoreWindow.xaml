<Window x:Class="SeeMusicApp.UploadScoreWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:SeeMusicApp"
        mc:Ignorable="d"
        Title="分享你的乐谱 - SeeMusic" Height="750" Width="550"
        WindowStartupLocation="CenterOwner" AllowsTransparency="True" WindowStyle="None"
        Background="Transparent" MouseLeftButtonDown="Window_MouseLeftButtonDown">

    <Border Background="White" CornerRadius="40" BorderThickness="1" BorderBrush="#F1F5F9">
        <Border.Effect><DropShadowEffect Color="Black" Opacity="0.1" BlurRadius="50" ShadowDepth="0"/></Border.Effect>
        
        <Grid Margin="45">
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="*"/>
                <RowDefinition Height="Auto"/>
            </Grid.RowDefinitions>

            <!-- 标题区 -->
            <StackPanel Grid.Row="0" Margin="0,0,0,30">
                <TextBlock Text="探索与分享" FontSize="26" FontWeight="Bold" Foreground="#1E293B"/>
                <TextBlock Text="在这里，让你的每一次扒谱都充满意义" FontSize="13" Foreground="#94A3B8" Margin="0,5,0,0"/>
            </StackPanel>

            <!-- 可滚动的表单 -->
            <ScrollViewer Grid.Row="1" VerticalScrollBarVisibility="Hidden">
                <StackPanel>
                    <TextBlock Text="乐谱标题" FontWeight="Bold" Foreground="#64748B" FontSize="13" Margin="0,0,0,10"/>
                    <TextBox x:Name="TxtTitle" Padding="15,14" Background="#F8FAFC" BorderThickness="0" Height="50" Margin="0,0,0,25">
                        <TextBox.Resources><Style TargetType="Border"><Setter Property="CornerRadius" Value="15"/></Style></TextBox.Resources>
                    </TextBox>

                    <Grid Margin="0,0,0,25">
                        <Grid.ColumnDefinitions><ColumnDefinition Width="*"/><ColumnDefinition Width="20"/><ColumnDefinition Width="*"/></Grid.ColumnDefinitions>
                        <StackPanel Grid.Column="0">
                            <TextBlock Text="作曲/艺术家" FontWeight="Bold" Foreground="#64748B" FontSize="13" Margin="0,0,0,10"/>
                            <TextBox x:Name="TxtArtist" Padding="15,14" Background="#F8FAFC" BorderThickness="0" Height="50">
                                <TextBox.Resources><Style TargetType="Border"><Setter Property="CornerRadius" Value="15"/></Style></TextBox.Resources>
                            </TextBox>
                        </StackPanel>
                        <StackPanel Grid.Column="2">
                            <TextBlock Text="乐谱价格 (元)" FontWeight="Bold" Foreground="#64748B" FontSize="13" Margin="0,0,0,10"/>
                            <TextBox x:Name="TxtPrice" Text="0" Padding="15,14" Background="#F8FAFC" BorderThickness="0" Height="50">
                                <TextBox.Resources><Style TargetType="Border"><Setter Property="CornerRadius" Value="15"/></Style></TextBox.Resources>
                            </TextBox>
                        </StackPanel>
                    </Grid>

                    <TextBlock Text="选择分类" FontWeight="Bold" Foreground="#64748B" FontSize="13" Margin="0,0,0,10"/>
                    <ComboBox x:Name="CboCategory" Padding="15,10" Background="#F8FAFC" BorderThickness="0" Height="50" Margin="0,0,0,25">
                        <ComboBox.Resources><Style TargetType="Border"><Setter Property="CornerRadius" Value="15"/></Style></ComboBox.Resources>
                        <ComboBoxItem Content="流行乐" IsSelected="True"/>
                        <ComboBoxItem Content="古典"/>
                        <ComboBoxItem Content="动漫"/>
                        <ComboBoxItem Content="爵士"/>
                    </ComboBox>

                    <!-- 自选封面预览区 (关键更新) -->
                    <TextBlock Text="封面预览 (自选图片)" FontWeight="Bold" Foreground="#64748B" FontSize="13" Margin="0,0,0,10"/>
                    <Border Height="180" Background="#F8FAFC" CornerRadius="20" Margin="0,0,0,25" ClipToBounds="True">
                        <Grid>
                            <Image x:Name="ImgCoverPreview" Stretch="UniformToFill" />
                            <Button Background="Transparent" BorderThickness="0" Click="BtnBrowseCover_Click" Cursor="Hand">
                                <StackPanel VerticalAlignment="Center">
                                    <TextBlock Text="&#xE722;" FontFamily="Segoe MDL2 Assets" FontSize="30" Foreground="#CBD5E1" HorizontalAlignment="Center"/>
                                    <TextBlock Text="点击添加封面" Foreground="#94A3B8" FontSize="12" Margin="0,10,0,0" HorizontalAlignment="Center"/>
                                </StackPanel>
                            </Button>
                        </Grid>
                    </Border>
                    <!-- 用来存路径，但不显示 -->
                    <TextBox x:Name="TxtCoverPath" Visibility="Collapsed"/>

                    <TextBlock Text="我的扒谱文件 (.pdf, .mid, .xml)" FontWeight="Bold" Foreground="#64748B" FontSize="13" Margin="0,0,0,10"/>
                    <Grid Height="50" Margin="0,0,0,25">
                        <Grid.ColumnDefinitions><ColumnDefinition Width="*"/><ColumnDefinition Width="Auto"/></Grid.ColumnDefinitions>
                        <TextBox x:Name="TxtFilePath" IsReadOnly="True" Padding="15,14" Background="#F8FAFC" BorderThickness="0">
                            <TextBox.Resources><Style TargetType="Border"><Setter Property="CornerRadius" Value="15,0,0,15"/></Style></TextBox.Resources>
                        </TextBox>
                        <Button Grid.Column="1" Content="选择文件" Click="BtnBrowse_Click" Width="100" Background="#E2E8F0" Foreground="#475569" FontWeight="Bold" BorderThickness="0" Cursor="Hand">
                            <Button.Resources><Style TargetType="Border"><Setter Property="CornerRadius" Value="0,15,15,0"/></Style></Button.Resources>
                        </Button>
                    </Grid>

                    <TextBlock Text="编配标签 (如：原版钢琴、简易版)" FontWeight="Bold" Foreground="#64748B" FontSize="13" Margin="0,0,0,10"/>
                    <TextBox x:Name="TxtTag" Padding="15,14" Background="#F8FAFC" BorderThickness="0" Height="50" Margin="0,0,0,25">
                        <TextBox.Resources><Style TargetType="Border"><Setter Property="CornerRadius" Value="15"/></Style></TextBox.Resources>
                    </TextBox>

                    <TextBlock Text="详情介绍" FontWeight="Bold" Foreground="#64748B" FontSize="13" Margin="0,0,0,10"/>
                    <TextBox x:Name="TxtDesc" Padding="15,14" Background="#F8FAFC" BorderThickness="0" Height="100" TextWrapping="Wrap" AcceptsReturn="True" Margin="0,0,0,20">
                        <TextBox.Resources><Style TargetType="Border"><Setter Property="CornerRadius" Value="15"/></Style></TextBox.Resources>
                    </TextBox>
                </StackPanel>
            </ScrollViewer>

            <!-- 操作区 -->
            <Grid Grid.Row="2" Margin="0,20,0,0">
                <Grid.ColumnDefinitions><ColumnDefinition Width="*"/><ColumnDefinition Width="20"/><ColumnDefinition Width="1.5*"/></Grid.ColumnDefinitions>
                <Button Grid.Column="0" Content="返回" Click="BtnCancel_Click" Background="#F1F5F9" Foreground="#64748B" BorderThickness="0" Height="55" FontWeight="Bold" Cursor="Hand">
                    <Button.Resources><Style TargetType="Border"><Setter Property="CornerRadius" Value="15"/></Style></Button.Resources>
                </Button>
                <Button Grid.Column="2" Content="立即发布" Click="BtnUpload_Click" Background="#1D3557" Foreground="White" BorderThickness="0" Height="55" FontWeight="Bold" Cursor="Hand">
                    <Button.Resources><Style TargetType="Border"><Setter Property="CornerRadius" Value="15"/></Style></Button.Resources>
                </Button>
            </Grid>
        </Grid>
    </Border>
</Window>
