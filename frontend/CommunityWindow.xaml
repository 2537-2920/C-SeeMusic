<Window x:Class="SeeMusicApp.CommunityWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:SeeMusicApp"
        mc:Ignorable="d"
        Title="乐谱社区 - SeeMusic" Height="880" Width="1400"
        WindowStartupLocation="CenterScreen" AllowsTransparency="True" WindowStyle="None"
        MouseLeftButtonDown="Window_MouseLeftButtonDown">

    <Window.Background>
        <LinearGradientBrush StartPoint="0,0" EndPoint="1,1">
            <GradientStop Color="#FCFCFC" Offset="0"/>
            <GradientStop Color="#F3F4F6" Offset="1"/>
        </LinearGradientBrush>
    </Window.Background>

    <Window.Resources>
        <BooleanToVisibilityConverter x:Key="BooleanToVisibilityConverter"/>
        <local:InverseBooleanToVisibilityConverter x:Key="InverseBooleanToVisibilityConverter"/>
        
        <!-- 同步最美卡片样式 -->
        <Style x:Key="ScoreCardStyle" TargetType="Border">
            <Setter Property="Background" Value="White"/>
            <Setter Property="CornerRadius" Value="24"/>
            <Setter Property="Padding" Value="15"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="Effect">
                <Setter.Value>
                    <DropShadowEffect Color="#8094A3B8" Opacity="0.1" BlurRadius="25" ShadowDepth="5"/>
                </Setter.Value>
            </Setter>
            <Style.Triggers>
                <Trigger Property="IsMouseOver" Value="True">
                    <Setter Property="Background" Value="#F8FAFC"/>
                </Trigger>
            </Style.Triggers>
        </Style>

        <Style x:Key="StatIconStyle" TargetType="TextBlock">
            <Setter Property="FontFamily" Value="Segoe MDL2 Assets"/>
            <Setter Property="FontSize" Value="13"/>
            <Setter Property="Foreground" Value="#94A3B8"/>
            <Setter Property="VerticalAlignment" Value="Center"/>
            <Setter Property="Margin" Value="0,0,5,0"/>
        </Style>

        <Style x:Key="CategoryButtonStyle" TargetType="Button">
            <Setter Property="Background" Value="#FAFAFA"/>
            <Setter Property="Foreground" Value="#64748B"/>
            <Setter Property="BorderBrush" Value="#E2E8F0"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="FontSize" Value="13"/>
            <Setter Property="FontWeight" Value="SemiBold"/>
            <Setter Property="Padding" Value="18,8"/>
            <Setter Property="Margin" Value="0,0,12,0"/>
            <Setter Property="RenderTransformOrigin" Value="0.5,0.5"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border x:Name="BtnBorder" Background="{TemplateBinding Background}" BorderBrush="{TemplateBinding BorderBrush}" BorderThickness="{TemplateBinding BorderThickness}" CornerRadius="12">
                            <Border.RenderTransform>
                                <ScaleTransform ScaleX="1" ScaleY="1"/>
                            </Border.RenderTransform>
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center" Margin="{TemplateBinding Padding}"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Trigger.EnterActions>
                                    <BeginStoryboard>
                                        <Storyboard>
                                            <DoubleAnimation Storyboard.TargetName="BtnBorder" Storyboard.TargetProperty="(UIElement.RenderTransform).(ScaleTransform.ScaleX)" To="1.05" Duration="0:0:0.15"/>
                                            <DoubleAnimation Storyboard.TargetName="BtnBorder" Storyboard.TargetProperty="(UIElement.RenderTransform).(ScaleTransform.ScaleY)" To="1.05" Duration="0:0:0.15"/>
                                            <ColorAnimation Storyboard.TargetName="BtnBorder" Storyboard.TargetProperty="(Border.Background).(SolidColorBrush.Color)" To="#F1F5F9" Duration="0:0:0.15"/>
                                        </Storyboard>
                                    </BeginStoryboard>
                                </Trigger.EnterActions>
                                <Trigger.ExitActions>
                                    <BeginStoryboard>
                                        <Storyboard>
                                            <DoubleAnimation Storyboard.TargetName="BtnBorder" Storyboard.TargetProperty="(UIElement.RenderTransform).(ScaleTransform.ScaleX)" To="1" Duration="0:0:0.15"/>
                                            <DoubleAnimation Storyboard.TargetName="BtnBorder" Storyboard.TargetProperty="(UIElement.RenderTransform).(ScaleTransform.ScaleY)" To="1" Duration="0:0:0.15"/>
                                            <ColorAnimation Storyboard.TargetName="BtnBorder" Storyboard.TargetProperty="(Border.Background).(SolidColorBrush.Color)" To="#FAFAFA" Duration="0:0:0.15"/>
                                        </Storyboard>
                                    </BeginStoryboard>
                                </Trigger.ExitActions>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
    </Window.Resources>

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <!-- 1. 顶部栏 (严格保持设计风格) -->
        <Border Grid.Row="0" Background="White" BorderBrush="#E2E8F0" BorderThickness="0,0,0,1">
            <Grid Margin="50,25">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="Auto"/>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="Auto"/>
                </Grid.ColumnDefinitions>

                <StackPanel Grid.Column="0" Orientation="Horizontal" VerticalAlignment="Center">
                    <Button Background="Transparent" BorderThickness="0" Cursor="Hand" Click="BtnGoHome_Click" ToolTip="返回主界面">
                        <Button.Template>
                            <ControlTemplate TargetType="Button">
                                <Border Background="Transparent">
                                    <StackPanel Orientation="Horizontal" VerticalAlignment="Center">
                                        <Border x:Name="LogoIcon" Width="32" Height="32" Background="#457b9d" CornerRadius="4" RenderTransformOrigin="0.5,0.5">
                                            <Border.RenderTransform>
                                                <TransformGroup>
                                                    <RotateTransform Angle="0"/>
                                                    <ScaleTransform ScaleX="1" ScaleY="1"/>
                                                </TransformGroup>
                                            </Border.RenderTransform>
                                            <TextBlock Text="&#xE8D6;" FontFamily="Segoe MDL2 Assets" Foreground="White" HorizontalAlignment="Center" VerticalAlignment="Center" FontSize="18"/>
                                        </Border>
                                        <TextBlock Text="SeeMusic" FontWeight="Bold" FontSize="20" Foreground="#1d3557" Margin="10,0,5,0" VerticalAlignment="Center"/>
                                    </StackPanel>
                                </Border>
                                <ControlTemplate.Triggers>
                                    <Trigger Property="IsMouseOver" Value="True">
                                        <Trigger.EnterActions>
                                            <BeginStoryboard>
                                                <Storyboard>
                                                    <DoubleAnimation Storyboard.TargetName="LogoIcon" Storyboard.TargetProperty="(UIElement.RenderTransform).(TransformGroup.Children)[0].(RotateTransform.Angle)" To="12" Duration="0:0:0.2"/>
                                                    <DoubleAnimation Storyboard.TargetName="LogoIcon" Storyboard.TargetProperty="(UIElement.RenderTransform).(TransformGroup.Children)[1].(ScaleTransform.ScaleX)" To="1.1" Duration="0:0:0.2"/>
                                                    <DoubleAnimation Storyboard.TargetName="LogoIcon" Storyboard.TargetProperty="(UIElement.RenderTransform).(TransformGroup.Children)[1].(ScaleTransform.ScaleY)" To="1.1" Duration="0:0:0.2"/>
                                                </Storyboard>
                                            </BeginStoryboard>
                                        </Trigger.EnterActions>
                                        <Trigger.ExitActions>
                                            <BeginStoryboard>
                                                <Storyboard>
                                                    <DoubleAnimation Storyboard.TargetName="LogoIcon" Storyboard.TargetProperty="(UIElement.RenderTransform).(TransformGroup.Children)[0].(RotateTransform.Angle)" To="0" Duration="0:0:0.2"/>
                                                    <DoubleAnimation Storyboard.TargetName="LogoIcon" Storyboard.TargetProperty="(UIElement.RenderTransform).(TransformGroup.Children)[1].(ScaleTransform.ScaleX)" To="1" Duration="0:0:0.2"/>
                                                    <DoubleAnimation Storyboard.TargetName="LogoIcon" Storyboard.TargetProperty="(UIElement.RenderTransform).(TransformGroup.Children)[1].(ScaleTransform.ScaleY)" To="1" Duration="0:0:0.2"/>
                                                </Storyboard>
                                            </BeginStoryboard>
                                        </Trigger.ExitActions>
                                    </Trigger>
                                </ControlTemplate.Triggers>
                            </ControlTemplate>
                        </Button.Template>
                    </Button>
                    <Line Y1="8" Y2="38" Stroke="#CBD5E1" StrokeThickness="1" Margin="15,0,20,0" VerticalAlignment="Center"/>
                    <StackPanel VerticalAlignment="Center">
                        <TextBlock Text="乐谱社区" FontWeight="Bold" FontSize="28" Foreground="#1E293B"/>
                        <TextBlock Text="SCORE COMMUNITY &amp; ASSETS" FontSize="11" Foreground="#94A3B8" FontWeight="Bold" Margin="0,4,0,0" />
                    </StackPanel>
                </StackPanel>

                <StackPanel Grid.Column="2" Orientation="Horizontal" VerticalAlignment="Center">
                    <TextBlock Text="当前版本: v2026.4" Foreground="#94A3B8" FontSize="11" FontWeight="SemiBold" VerticalAlignment="Center" Margin="0,0,25,0"/>
                    <Ellipse Width="40" Height="40" Fill="#A8DADC" Margin="0,0,12,0" Stroke="White" StrokeThickness="2"/>
                    <TextBlock Text="灵感创作者" FontWeight="Bold" Foreground="#1E293B" VerticalAlignment="Center"/>
                </StackPanel>
            </Grid>
        </Border>

        <!-- 2. 主内容区域 -->
        <Grid Grid.Row="1" Margin="40,20">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="450"/>
            </Grid.ColumnDefinitions>

            <!-- 左侧发现区 -->
            <Grid Grid.Column="0" Margin="0,0,20,0">
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="*"/>
                </Grid.RowDefinitions>

                <Grid Grid.Row="0" Margin="10,0,10,25">
                    <StackPanel>
                        <TextBlock Text="探索发现" FontSize="24" FontWeight="Bold" Foreground="#1E293B"/>
                        <TextBlock Text="从数千份高质量编配中找到你的最爱" FontSize="14" Foreground="#94A3B8" Margin="0,5,0,0"/>
                    </StackPanel>
                    <Button HorizontalAlignment="Right" Background="#1D3557" Foreground="White" Padding="25,12" FontWeight="Bold" BorderThickness="0" Click="BtnUpload_Click">
                        <Button.Resources><Style TargetType="Border"><Setter Property="CornerRadius" Value="14"/></Style></Button.Resources>
                        <StackPanel Orientation="Horizontal">
                            <TextBlock Text="&#xE11C;" FontFamily="Segoe MDL2 Assets" VerticalAlignment="Center" Margin="0,0,10,0"/>
                            <TextBlock Text="分享乐谱"/>
                        </StackPanel>
                    </Button>
                </Grid>

                <StackPanel Grid.Row="1" Orientation="Horizontal" Margin="10,0,0,25">
                    <Button Content="精选" Style="{StaticResource CategoryButtonStyle}" Background="#E2E8F0" Foreground="#1D3557" Click="FilterButton_Click"/>
                    <Button Content="流行乐" Style="{StaticResource CategoryButtonStyle}" Click="FilterButton_Click"/>
                    <Button Content="古典" Style="{StaticResource CategoryButtonStyle}" Click="FilterButton_Click"/>
                    <Button Content="动漫" Style="{StaticResource CategoryButtonStyle}" Click="FilterButton_Click"/>
                    <Button Content="爵士" Style="{StaticResource CategoryButtonStyle}" Click="FilterButton_Click"/>
                </StackPanel>

                <ScrollViewer Grid.Row="2" VerticalScrollBarVisibility="Hidden">
                    <ItemsControl x:Name="ScoresItemsControl">
                        <ItemsControl.ItemsPanel><ItemsPanelTemplate><WrapPanel/></ItemsPanelTemplate></ItemsControl.ItemsPanel>
                        <ItemsControl.ItemTemplate>
                            <DataTemplate>
                                <Border Style="{StaticResource ScoreCardStyle}" Width="245" Margin="10,0,20,30" MouseLeftButtonDown="ScoreCard_Click" Tag="{Binding}">
                                    <StackPanel>
                                        <!-- 封面 -->
                                        <Border Height="245" Background="#F1F5F9" CornerRadius="20" Margin="0,0,0,15" ClipToBounds="True">
                                            <Grid>
                                                <Image Source="{Binding FullCoverUrl}" Stretch="UniformToFill" Visibility="{Binding HasCover, Converter={StaticResource BooleanToVisibilityConverter}}"/>
                                                <TextBlock Text="{Binding TitleAbbreviation}" FontSize="60" FontWeight="Bold" Foreground="#CBD5E1" HorizontalAlignment="Center" VerticalAlignment="Center" Visibility="{Binding HasCover, Converter={StaticResource InverseBooleanToVisibilityConverter}}"/>
                                            </Grid>
                                        </Border>
                                        <!-- 信息 -->
                                        <TextBlock Text="{Binding Title}" FontWeight="Bold" FontSize="17" Foreground="#1E293B" MaxHeight="25" TextTrimming="CharacterEllipsis"/>
                                        <StackPanel Orientation="Horizontal" Margin="0,5,0,12">
                                            <Border Width="18" Height="18" CornerRadius="9" Background="#A8DADC" Margin="0,0,8,0"/>
                                            <TextBlock Text="{Binding OwnerName}" FontSize="12" Foreground="#94A3B8" VerticalAlignment="Center"/>
                                        </StackPanel>
                                        <!-- 数据 -->
                                        <Grid>
                                            <Grid.ColumnDefinitions><ColumnDefinition Width="*"/><ColumnDefinition Width="Auto"/></Grid.ColumnDefinitions>
                                            <TextBlock Text="{Binding PriceString}" FontWeight="Bold" Foreground="#457b9d" FontSize="16"/>
                                            <StackPanel Grid.Column="1" Orientation="Horizontal">
                                                <StackPanel Orientation="Horizontal" Margin="0,0,10,0">
                                                    <TextBlock Text="&#xEB52;" Style="{StaticResource StatIconStyle}"/>
                                                    <TextBlock Text="{Binding FavoriteCount}" FontSize="12" Foreground="#94A3B8"/>
                                                </StackPanel>
                                                <StackPanel Orientation="Horizontal">
                                                    <TextBlock Text="&#xE896;" Style="{StaticResource StatIconStyle}"/>
                                                    <TextBlock Text="{Binding DownloadCountDisplay}" FontSize="12" Foreground="#94A3B8"/>
                                                </StackPanel>
                                            </StackPanel>
                                        </Grid>
                                    </StackPanel>
                                </Border>
                            </DataTemplate>
                        </ItemsControl.ItemTemplate>
                    </ItemsControl>
                </ScrollViewer>
            </Grid>

            <!-- 右侧详情 (核心重构区) -->
            <Border Grid.Column="1" Background="White" CornerRadius="36" Padding="35" BorderBrush="#E2E8F0" BorderThickness="1">
                <Border.Effect><DropShadowEffect Color="#8094A3B8" Opacity="0.15" BlurRadius="40" ShadowDepth="10"/></Border.Effect>
                <Grid>
                    <!-- 空态占位 -->
                    <StackPanel x:Name="DetailEmptyState" VerticalAlignment="Center" HorizontalAlignment="Center">
                        <TextBlock Text="&#xEC4A;" FontFamily="Segoe MDL2 Assets" FontSize="90" Foreground="#F3F4F6" Margin="0,0,0,25"/>
                        <TextBlock Text="欢迎来到社区，请点击左侧浏览乐谱" Foreground="#CBD5E1" FontSize="15" HorizontalAlignment="Center"/>
                    </StackPanel>

                    <!-- 详情内容 (分层布局，确保底部评论框可用) -->
                    <Grid x:Name="DetailContentState" Visibility="Collapsed">
                        <Grid.RowDefinitions>
                            <RowDefinition Height="*"/>    <!-- 可滚动内容区 -->
                            <RowDefinition Height="Auto"/> <!-- 固定评论输入区 -->
                        </Grid.RowDefinitions>

                        <!-- 第1层：可滚动的内容 -->
                        <ScrollViewer Grid.Row="0" VerticalScrollBarVisibility="Auto" Padding="0,0,10,0">
                            <StackPanel Margin="0,0,0,20">
                                <TextBlock x:Name="DetailTitle" Text="乐谱名称" FontSize="28" FontWeight="Bold" Foreground="#1E293B"/>
                                <TextBlock x:Name="DetailAuthor" Text="发布者姓名" FontSize="15" Foreground="#94A3B8" Margin="0,8,0,30"/>

                                <!-- 封面大图 (支持自选展示) -->
                                <Border Height="300" Background="#F8FAFC" CornerRadius="24" Margin="0,0,0,30" ClipToBounds="True">
                                    <Grid>
                                        <Image x:Name="DetailCoverImage" Stretch="UniformToFill" />
                                        <TextBlock x:Name="DetailCoverText" Text="PREVIEW" FontSize="100" FontWeight="Bold" Foreground="#E2E8F0" HorizontalAlignment="Center" VerticalAlignment="Center"/>
                                    </Grid>
                                </Border>

                                <!-- 互动统计面板 -->
                                <Grid Margin="0,0,0,35">
                                    <Grid.ColumnDefinitions><ColumnDefinition Width="*"/><ColumnDefinition Width="*"/><ColumnDefinition Width="*"/></Grid.ColumnDefinitions>
                                    <StackPanel Grid.Column="0" HorizontalAlignment="Center">
                                        <TextBlock Text="点赞量" FontSize="11" Foreground="#94A3B8" HorizontalAlignment="Center"/>
                                        <TextBlock Text="1.2k" x:Name="DetailStatLikes" FontWeight="Bold" FontSize="18" Foreground="#1E293B" Margin="0,5,0,0" HorizontalAlignment="Center"/>
                                    </StackPanel>
                                    <StackPanel Grid.Column="1" HorizontalAlignment="Center">
                                        <TextBlock Text="下载量" FontSize="11" Foreground="#94A3B8" HorizontalAlignment="Center"/>
                                        <TextBlock Text="856" x:Name="DetailStatDownloads" FontWeight="Bold" FontSize="18" Foreground="#1E293B" Margin="0,5,0,0" HorizontalAlignment="Center"/>
                                    </StackPanel>
                                    <StackPanel Grid.Column="2" HorizontalAlignment="Center">
                                        <TextBlock Text="转发量" FontSize="11" Foreground="#94A3B8" HorizontalAlignment="Center"/>
                                        <TextBlock Text="128" x:Name="DetailStatShares" FontWeight="Bold" FontSize="18" Foreground="#1E293B" Margin="0,5,0,0" HorizontalAlignment="Center"/>
                                    </StackPanel>
                                </Grid>

                                <!-- 操作按钮 -->
                                <Grid Margin="0,0,0,35">
                                    <Grid.ColumnDefinitions><ColumnDefinition Width="*"/><ColumnDefinition Width="70"/></Grid.ColumnDefinitions>
                                    <Button Background="#1D3557" Foreground="White" FontWeight="Bold" FontSize="16" Height="60" BorderThickness="0" Cursor="Hand">
                                        <Button.Resources><Style TargetType="Border"><Setter Property="CornerRadius" Value="18"/></Style></Button.Resources>
                                        <StackPanel Orientation="Horizontal">
                                            <TextBlock Text="&#xE896;" FontFamily="Segoe MDL2 Assets" VerticalAlignment="Center" Margin="0,0,12,0" FontSize="20"/>
                                            <TextBlock Text="获取完整乐谱"/>
                                        </StackPanel>
                                    </Button>
                                    <Button Grid.Column="1" Background="#F1F5F9" BorderThickness="0" Height="60" Margin="15,0,0,0" Click="BtnFavorite_Click" Cursor="Hand">
                                        <Button.Resources><Style TargetType="Border"><Setter Property="CornerRadius" Value="18"/></Style></Button.Resources>
                                        <TextBlock Text="&#xEB52;" FontFamily="Segoe MDL2 Assets" FontSize="24" Foreground="#64748B" x:Name="IconFavorite"/>
                                    </Button>
                                </Grid>

                                <!-- 评论列表 (此处仅列表项展示) -->
                                <TextBlock Text="热门评论" FontWeight="Bold" FontSize="18" Margin="0,0,0,25" Foreground="#1E293B"/>
                                <ItemsControl x:Name="CommentsItemsControl">
                                    <ItemsControl.ItemTemplate>
                                        <DataTemplate>
                                            <Grid Margin="0,0,0,25">
                                                <Grid.ColumnDefinitions><ColumnDefinition Width="Auto"/><ColumnDefinition Width="*"/></Grid.ColumnDefinitions>
                                                <Ellipse Width="40" Height="40" Fill="#F8FAFC" VerticalAlignment="Top"/>
                                                <StackPanel Grid.Column="1" Margin="15,0,0,0">
                                                    <TextBlock Text="{Binding Username}" FontWeight="Bold" FontSize="14" Foreground="#334155"/>
                                                    <TextBlock Text="{Binding Content}" FontSize="14" Foreground="#64748B" TextWrapping="Wrap" Margin="0,8,0,0" LineHeight="22"/>
                                                    <TextBlock Text="{Binding RelativeTime}" FontSize="11" Foreground="#94A3B8" Margin="0,8,0,0"/>
                                                </StackPanel>
                                            </Grid>
                                        </DataTemplate>
                                    </ItemsControl.ItemTemplate>
                                </ItemsControl>
                            </StackPanel>
                        </ScrollViewer>

                        <!-- 第2层：固定在底部、绝对不被顶开的评论区 -->
                        <Border Grid.Row="1" Background="White" Padding="0,15,0,0" BorderBrush="#F1F5F9" BorderThickness="0,1,0,0" VerticalAlignment="Bottom">
                            <Grid>
                                <Grid.ColumnDefinitions><ColumnDefinition Width="*"/><ColumnDefinition Width="Auto"/></Grid.ColumnDefinitions>
                                <TextBox x:Name="CommentInput" Grid.Column="0" Background="#F8FAFC" BorderThickness="0" Padding="18,16" Foreground="#333" FontSize="14" Height="55" KeyDown="CommentInput_KeyDown">
                                    <TextBox.Resources><Style TargetType="Border"><Setter Property="CornerRadius" Value="16"/></Style></TextBox.Resources>
                                </TextBox>
                                <TextBlock Text="发表你的创作见解..." Foreground="#CBD5E1" FontSize="14" Margin="22,0,0,0" VerticalAlignment="Center" IsHitTestVisible="False">
                                    <TextBlock.Style>
                                        <Style TargetType="TextBlock">
                                            <Setter Property="Visibility" Value="Collapsed"/>
                                            <Style.Triggers>
                                                <DataTrigger Binding="{Binding Text, ElementName=CommentInput}" Value="">
                                                    <Setter Property="Visibility" Value="Visible"/>
                                                </DataTrigger>
                                            </Style.Triggers>
                                        </Style>
                                    </TextBlock.Style>
                                </TextBlock>
                                <Button Grid.Column="1" Width="55" Height="55" Margin="12,0,0,0" Background="#1D3557" Foreground="White" BorderThickness="0" Click="BtnSendComment_Click" Cursor="Hand">
                                    <Button.Resources><Style TargetType="Border"><Setter Property="CornerRadius" Value="16"/></Style></Button.Resources>
                                    <TextBlock Text="&#xE724;" FontFamily="Segoe MDL2 Assets" FontSize="18" HorizontalAlignment="Center"/>
                                </Button>
                            </Grid>
                        </Border>
                    </Grid>
                </Grid>
            </Border>
        </Grid>
    </Grid>
</Window>